// Copy this file to config.js and fill in your API keys.
// config.js is gitignored — never commit it.
//
// For GitHub Pages deployment, add TMDB_KEY and HF_TOKEN as repository secrets
// (Settings → Secrets and variables → Actions) — the deploy workflow writes
// config.js automatically from those secrets.

var apiKeyMD = "api_key=YOUR_TMDB_KEY_HERE"
var apiKeyHF = "hf_YOUR_HF_TOKEN_HERE"
var HF_BASE = "https://api-inference.huggingface.co/models/"
